; ESP32 one-chunk Minecraft server (bareiron-based)
; Framework MUST be espidf — Arduino will not work.

[platformio]
default_envs = esp32dev

[env]
platform = espressif32
framework = espidf
monitor_speed = 115200
monitor_filters = esp32_exception_decoder

; bareiron trips a few pedantic warnings treated as errors in stock IDF flags
build_unflags =
	-Werror
	-Werror=all
build_flags =
	-Wno-error=parentheses
	-O2
	-DBOARD_HAS_PSRAM=0

[env:esp32dev]
board = esp32dev
board_build.f_cpu = 240000000L

[env:esp32-s3]
board = esp32-s3-devkitc-1
board_build.f_cpu = 240000000L

[env:esp32-c3]
board = esp32-c3-devkitm-1
board_build.f_cpu = 160000000L
; C3 is tighter on RAM — keep VIEW_DISTANCE 0 / MAX_PLAYERS 1
